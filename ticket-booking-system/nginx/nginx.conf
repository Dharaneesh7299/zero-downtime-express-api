events {}

http {
    resolver 127.0.0.11 valid=5s;

    server {
        listen 80;

        location / {
            set $backend http://blue:3600;
            proxy_pass $backend;
        }
    }
}